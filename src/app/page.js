/**
 * Es la página raíz /. Hace una redirección a /home/descubre-connexa (en next.config.mjs), así que no es necesario que devuelva nada
 */
export default async function RootPage () {
  return (
    <div />
  )
}
